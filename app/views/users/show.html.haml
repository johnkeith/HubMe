/ %h2 Languages I've Used
/ %ul.profile-languages
/   - @languages.each do |lang|
/     %li= lang.name

/ %h2 Percentages of My Repos in Each Language


/ %h2 My Repos
/ - @repos.each do |repo|
/   %ul.profile-repos
/     %li= repo.name
/     %li= repo.html_url

/ loop through each language
/ create panel.panel-default for each language
/ within that panel, create ul with lis of each repo matching the language id

/ @languages.each do |lang|
/   lang = Language.find_by(name: lang)
/   repo_languages.where(language_id: lang.id)
/ .row
/   .col-md-12.text-center
/     %img{src: "#{current_user.avatar_url}", class: "img-circle", width: "150px", height: "150px"}
.row
  .col-md-4
    .portfolio-header-row
      %img{src: "#{current_user.avatar_url}", class: "img-circle portfolio-profile"}


  / .col-md-6
  /   #accordion.panel-group
  /     -@languages.each do |lang|
  /       .panel.panel-default
  /         %a{"data-parent" => "#accordion", "data-toggle" => "collapse", href: "#collapse#{lang.id}"}
  /           .panel-heading
  /             %h4.panel-title= lang.name
  /         .panel-collapse.collapse{:id => "collapse#{lang.id}"}
  /           .panel-body
  /             %ul
  /               - current_user.repo_languages.where(language_id: lang.id).each do |repo_lang|
  /                 %li
  /                   %a{href: repo_lang.repo.html_url}= repo_lang.repo.name
  .col-md-6.col-md-offset-3
    - @langs_as_percents.each do |lang, amount|
      .profile-languages-as-percents
        .lang-name= "#{lang}"
        .lang-percent{style: "width: 2#{amount.round}px;"}= "#{amount.round(2)}%"

    / - @langs_in_bytes.each do |lang, amount|
    /   %ul.profile-languages-as-percents
    /     %li= "#{lang}: "
    /     %li= amount
